(* Generated by tptp2coqp *)
Require Import Setoid.
From Completion Require Import Plugin.

(* axioms *)
Parameter G : Set.
Parameter a : G.
Parameter b : G.
Parameter c : G.
Parameter d : G.
Parameter f : G -> G.
Axiom a_is_c : a = c.
Axiom cd : (f c) = d.
Axiom ab : (f a) = b.

Complete a_is_c cd ab : a b c d f : hint
  for ((f a) = d).

(* Goal *)
Theorem check : (f a) = d.
Proof.
  autorewrite with hint.
  reflexivity.
Qed.

Check check.

